@Library('hello-shared-library@master') _

pipeline {
    agent any
    stages{
        stage("Hello"){
            steps{
              echo "hello"
                // hello name: "roberto"
            }
        }
    }
}